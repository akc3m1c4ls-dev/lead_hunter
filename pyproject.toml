[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lead-hunter"
version = "0.1.0"
description = "AI-powered local business lead generation agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
